name: AI Hardware Daily to Feishu

on:
  workflow_dispatch:

jobs:
  send-daily:
    runs-on: ubuntu-latest

    steps:
      - name: Send message to Feishu
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        run: |
          TODAY=$(date +"%Y-%m-%d")

          cat > payload.json <<EOF
          {
            "msg_type": "text",
            "content": {
              "text": "🤖 AI硬件日报｜${TODAY}\n\n【一句话总览】\nAI 硬件正在从拼算力，转向拼效率和落地。\n\n🌍 全球 AI 硬件\n1️⃣（示例）AI 芯片开始更强调能效比\n- 重点从\"更强\"变成\"更省\"\n\n🇨🇳 中国 AI 硬件\n2️⃣（示例）国产算力进入真实应用阶段\n- 工程化能力成为关键\n\n📈 趋势判断\nAI 硬件正在从实验室走向产品化。"
            }
          }
          EOF

          curl -X POST "$FEISHU_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d @payload.json
